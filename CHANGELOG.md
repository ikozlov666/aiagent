# Changelog — AI Agent Platform

## [Фаза 2] — 2026-02-18

### ✅ Добавлено

#### Файловое дерево
- Древовидная структура файлов проекта
- Иконки для разных типов файлов
- Автоматическое обновление каждые 10 секунд
- API endpoint `/api/projects/{id}/files?tree=true`
- Метод `list_files_tree()` в DockerManager

#### Monaco Editor
- Интеграция @monaco-editor/react
- Подсветка синтаксиса для 15+ языков
- Чтение и сохранение файлов
- Автоматическое определение языка по расширению
- API endpoint `/api/projects/{id}/files/write` (POST)

#### xterm.js Терминал
- WebSocket терминал `/ws/terminal/{project_id}`
- Выполнение команд в контейнере в реальном времени
- Поддержка Ctrl+C, Backspace
- Темная тема терминала
- Индикатор подключения

#### Улучшенное превью
- Автоматическое обнаружение запущенных dev-серверов
- Проверка портов 3000, 5173, 8080, 5000, 8000, 4000
- Улучшенный UI с индикаторами загрузки
- Автообновление портов через WebSocket

#### Resizable Panels
- Библиотека react-resizable-panels
- Изменяемые размеры всех панелей
- Минимальные и максимальные размеры
- Плавные переходы

### 🔒 Безопасность

- ✅ Защита от path traversal (валидация путей файлов)
- ✅ Ограничение размера файлов (10MB для записи, 1MB для редактора)
- ✅ Pydantic модели для валидации API запросов
- ✅ Улучшенная обработка ошибок

### 🐛 Исправлено

- Исправлен race condition в FileTree (useEffect зависимости)
- Улучшена обработка ввода в терминале
- Исправлены синтаксические ошибки в backend

### 📝 Технические детали

**Новые зависимости:**
- `@xterm/xterm`, `@xterm/addon-fit`, `@xterm/addon-web-links`
- `@monaco-editor/react`, `monaco-editor`
- `react-resizable-panels`

**Новые API endpoints:**
- `GET /api/projects/{id}/files?tree=true` — древовидная структура
- `POST /api/projects/{id}/files/write` — запись файлов
- `WS /ws/terminal/{project_id}` — WebSocket терминал

**Новые методы DockerManager:**
- `list_files_tree()` — построение дерева файлов
- `check_dev_server()` — проверка запущенного сервера
- `find_running_servers()` — поиск всех запущенных серверов

---

## [Фаза 1] — Ранее

### ✅ Фундамент
- Docker-образ sandbox
- FastAPI бэкенд с WebSocket
- Agent Engine с tool calling
- React фронтенд
- Мультимодельный LLM Router
