# 🔍 Code Review — AI Agent Platform

**Дата:** 2026-02-18  
**Область:** Фаза 2 (FileTree + Monaco Editor)

---

## 🔴 Критические проблемы

### 1. **Path Traversal уязвимость** (Backend)

**Файл:** `backend/main.py:186-207`

**Проблема:**
```python
@app.post("/api/projects/{project_id}/files/write")
async def write_file(project_id: str, req: dict):
    filepath = req.get("filepath")
    # ❌ Нет валидации! Можно записать в /etc/passwd
    await docker_manager.write_file(project_id, f"/workspace/{filepath}", content)
```

**Риск:** Злоумышленник может записать файлы за пределами `/workspace`:
- `filepath="../../../etc/passwd"` → запись в системные файлы
- `filepath="../../../../root/.ssh/id_rsa"` → доступ к приватным ключам

**Решение:**
```python
import os

def validate_filepath(filepath: str) -> str:
    """Validate and sanitize filepath to prevent path traversal."""
    # Remove any leading slashes and normalize
    normalized = os.path.normpath(filepath).lstrip('/')
    
    # Check for path traversal attempts
    if '..' in normalized or normalized.startswith('/'):
        raise HTTPException(400, "Invalid filepath: path traversal detected")
    
    # Ensure it's within workspace
    full_path = os.path.join("/workspace", normalized)
    if not full_path.startswith("/workspace/"):
        raise HTTPException(400, "Invalid filepath: outside workspace")
    
    return normalized
```

**Также применить к:**
- `read_file` endpoint
- `list_files` endpoint (параметр `path`)

---

### 2. **Отсутствие валидации размера файлов**

**Проблема:** Нет ограничений на размер загружаемых/читаемых файлов.

**Риск:**
- DoS через загрузку огромных файлов (10GB+)
- Переполнение памяти при чтении больших файлов
- Замедление работы системы

**Решение:**
```python
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB

@app.post("/api/projects/{project_id}/files/write")
async def write_file(project_id: str, req: dict):
    content = req.get("content", "")
    if len(content.encode('utf-8')) > MAX_FILE_SIZE:
        raise HTTPException(413, f"File too large. Max size: {MAX_FILE_SIZE / 1024 / 1024}MB")
```

---

### 3. **Race condition в FileTree**

**Файл:** `frontend/src/components/FileTree/FileTree.jsx:94-99`

**Проблема:**
```javascript
useEffect(() => {
  loadTree()
  const interval = setInterval(loadTree, 5000)
  return () => clearInterval(interval)
}, [projectId]) // ❌ loadTree не в зависимостях!
```

**Риск:** 
- `loadTree` может использовать устаревший `projectId`
- При изменении `projectId` старый интервал может продолжать работать

**Решение:**
```javascript
useEffect(() => {
  if (!projectId) return
  
  let cancelled = false
  
  const load = async () => {
    if (cancelled) return
    // ... load logic
  }
  
  load()
  const interval = setInterval(load, 5000)
  
  return () => {
    cancelled = true
    clearInterval(interval)
  }
}, [projectId])
```

---

## ⚠️ Важные проблемы

### 4. **Неэффективный polling**

**Проблема:** FileTree обновляется каждые 5 секунд, даже если ничего не изменилось.

**Решение:**
- Использовать WebSocket для уведомлений об изменениях файлов
- Или увеличить интервал до 30 секунд
- Или добавить кнопку "Обновить" вместо автообновления

---

### 5. **Нет debounce для сохранения**

**Файл:** `frontend/src/components/Editor/Editor.jsx`

**Проблема:** Пользователь может нажать "Сохранить" несколько раз подряд.

**Решение:**
```javascript
const saveFile = useMemo(
  () => debounce(async () => {
    // ... save logic
  }, 300),
  [projectId, filepath, content]
)
```

**Или лучше:** Автосохранение через debounce + индикатор "Несохраненные изменения".

---

### 6. **Нет обработки больших файлов в Editor**

**Проблема:** Monaco Editor может зависнуть на файлах > 1MB.

**Решение:**
```javascript
const MAX_EDITOR_FILE_SIZE = 1024 * 1024 // 1MB

if (content.length > MAX_EDITOR_FILE_SIZE) {
  return (
    <div>Файл слишком большой для редактирования ({content.length} байт)</div>
  )
}
```

---

### 7. **Нет валидации типов в API**

**Файл:** `backend/main.py:186-207`

**Проблема:**
```python
async def write_file(project_id: str, req: dict):  # ❌ dict вместо Pydantic модели
    filepath = req.get("filepath")  # Может быть None
    content = req.get("content", "")  # Может быть не строкой
```

**Решение:**
```python
from pydantic import BaseModel

class WriteFileRequest(BaseModel):
    filepath: str
    content: str

@app.post("/api/projects/{project_id}/files/write")
async def write_file(project_id: str, req: WriteFileRequest):
    # Теперь типы гарантированы
```

---

### 8. **Потенциальная утечка памяти в list_files_tree**

**Файл:** `backend/docker_manager/manager.py:200-260`

**Проблема:** При большом количестве файлов `path_map` может расти без ограничений.

**Решение:** Добавить лимит на глубину/количество файлов:
```python
MAX_TREE_DEPTH = 10
MAX_TREE_FILES = 1000

if len(files) > MAX_TREE_FILES:
    # Вернуть упрощенную структуру или ошибку
```

---

## 💡 Улучшения производительности

### 9. **Оптимизация рендеринга FileTree**

**Проблема:** При большом дереве файлов React перерисовывает всё дерево.

**Решение:**
- Использовать `React.memo` для `FileTreeNode`
- Виртуализация для больших списков (react-window)
- Ленивая загрузка поддеревьев

```javascript
const FileTreeNode = React.memo(({ node, level, onFileSelect }) => {
  // ...
}, (prev, next) => prev.node.path === next.node.path && prev.expanded === next.expanded)
```

---

### 10. **Кеширование структуры файлов**

**Проблема:** Каждый запрос выполняет `find` в контейнере.

**Решение:**
- Кешировать результат на 1-2 секунды
- Инвалидировать кеш при изменениях (через WebSocket)

---

## 🔒 Безопасность

### 11. **Нет rate limiting**

**Риск:** DoS через множественные запросы к API.

**Решение:**
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.post("/api/projects/{project_id}/files/write")
@limiter.limit("10/minute")  # 10 запросов в минуту
async def write_file(...):
    ...
```

---

### 12. **Логирование чувствительных данных**

**Проблема:** В логах могут попадать содержимое файлов.

**Решение:** Не логировать содержимое файлов, только метаданные.

---

## 🐛 Баги

### 13. **Ошибка при пустом дереве**

**Файл:** `backend/docker_manager/manager.py:200-260`

**Проблема:** Если `files` пустой, `tree.children` будет пустым, но структура вернётся.

**Решение:** Проверить и вернуть пустое дерево корректно.

---

### 14. **Нет обработки бинарных файлов**

**Проблема:** Попытка прочитать бинарный файл как текст приведёт к ошибке.

**Решение:**
```python
def is_text_file(filepath: str) -> bool:
    text_extensions = {'.txt', '.js', '.py', '.html', '.css', ...}
    return any(filepath.endswith(ext) for ext in text_extensions)

if not is_text_file(filepath):
    raise HTTPException(400, "Binary files cannot be edited")
```

---

### 15. **Нет обработки специальных символов в путях**

**Проблема:** Имена файлов с `'`, `"`, `\n` могут сломать команды bash.

**Решение:** Использовать параметризованные команды или экранирование.

---

## 📝 Рекомендации по коду

### 16. **Добавить типизацию (TypeScript)**

**Рекомендация:** Мигрировать на TypeScript для лучшей типобезопасности.

---

### 17. **Разделить на модули**

**Проблема:** `main.py` становится слишком большим (377 строк).

**Решение:** Вынести endpoints в отдельные модули:
```
backend/
  api/
    routes/
      projects.py
      files.py
      chat.py
```

---

### 18. **Добавить тесты**

**Рекомендация:** 
- Unit тесты для `list_files_tree`
- Integration тесты для API endpoints
- E2E тесты для FileTree + Editor

---

### 19. **Улучшить обработку ошибок**

**Проблема:** Много `try/except` без детального логирования.

**Решение:**
```python
import logging
logger = logging.getLogger(__name__)

try:
    ...
except Exception as e:
    logger.error(f"Failed to write file {filepath}: {e}", exc_info=True)
    raise HTTPException(500, "Internal server error")
```

---

### 20. **Добавить индикатор "Несохраненные изменения"**

**Рекомендация:** Показывать предупреждение при закрытии файла с несохраненными изменениями.

---

## ✅ Приоритет исправлений

### Критично (сделать немедленно):
1. ✅ Path traversal защита
2. ✅ Валидация размера файлов
3. ✅ Исправить race condition в FileTree

### Важно (сделать в ближайшее время):
4. ✅ Pydantic модели для API
5. ✅ Debounce для сохранения
6. ✅ Обработка больших файлов
7. ✅ Rate limiting

### Желательно (можно отложить):
8. Оптимизация производительности
9. Кеширование
10. Тесты

---

## 📊 Метрики качества кода

| Метрика | Текущее | Целевое |
|---------|---------|---------|
| Покрытие тестами | 0% | 70%+ |
| Критические уязвимости | 3 | 0 |
| Code smells | 5+ | < 2 |
| Дублирование кода | Среднее | Низкое |
| Типизация | Частичная | Полная (TS) |

---

**Следующие шаги:**
1. Исправить критические проблемы безопасности
2. Добавить валидацию и обработку ошибок
3. Оптимизировать производительность
4. Добавить тесты
