# 🤖 AI Agent Platform

Среда разработки с ИИ-агентом, который видит проект, выполняет команды в изолированном Docker-контейнере, управляет браузером и показывает всё в реальном времени.

## Быстрый старт

### 1. Клонируй и настрой

```bash
cd ai-agent-platform
cp .env.example .env
```

Открой `.env` и добавь хотя бы один API-ключ. Рекомендую начать с DeepSeek — самый дешёвый:
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

### 2. Собери Docker-образ sandbox

```bash
cd docker/sandbox
docker build -t ai-agent-sandbox:latest .
cd ../..
```

### 3. Запусти платформу

**Вариант A: через Docker Compose (рекомендуется)**
```bash
docker-compose up --build
```

**Вариант B: локально (для разработки)**

Терминал 1 — Backend:
```bash
cd backend
pip install -r requirements.txt
python main.py
```

Терминал 2 — Frontend:
```bash
cd frontend
npm install
npm run dev
```

### 4. Открой в браузере

```
http://localhost:5173
```

## Как это работает

```
Ты → Чат → FastAPI → Agent Engine → LLM (DeepSeek/Qwen/Claude)
                                       ↓
                              Tool calls (write_file, execute_command, ...)
                                       ↓
                              Docker Sandbox (изолированный контейнер)
                                       ↓
                              Результат → WebSocket → UI (реальное время)
```

1. Создаёшь проект → поднимается Docker-контейнер (Ubuntu + Node + Python)
2. Пишешь задачу в чат → агент планирует и выполняет через tool calls
3. Каждый шаг стримится через WebSocket → видишь в панели активности
4. Результат доступен в превью (iframe с dev-сервером из контейнера)

## Структура проекта

```
ai-agent-platform/
├── docker/
│   └── sandbox/           # Docker-образ песочницы
│       ├── Dockerfile
│       ├── entrypoint.sh
│       └── supervisord.conf
├── backend/
│   ├── main.py            # FastAPI + WebSocket + REST API
│   ├── config.py          # Настройки из .env
│   ├── agents/
│   │   ├── engine.py      # Agent Loop (ядро)
│   │   └── tools/
│   │       ├── definitions.py  # Описания tools для LLM
│   │       └── executor.py     # Выполнение tools в sandbox
│   ├── docker_manager/
│   │   └── manager.py     # Управление Docker-контейнерами
│   └── llm/
│       └── router.py      # Мультимодельный LLM Router
├── frontend/
│   ├── src/
│   │   ├── App.jsx         # Главный компонент (layout)
│   │   ├── components/
│   │   │   ├── Chat/       # Чат с агентом
│   │   │   └── AgentActivity/  # Панель шагов агента
│   │   ├── hooks/
│   │   │   └── useWebSocket.js  # WebSocket подключение
│   │   └── stores/
│   │       └── useStore.js      # Zustand state
│   └── vite.config.js
├── docker-compose.yml
├── .env.example
└── README.md
```

## Порты

| Порт | Сервис |
|------|--------|
| 5173 | Frontend (Vite) |
| 8000 | Backend (FastAPI) |
| 6379 | Redis |
| dynamic | noVNC (per sandbox) |
| dynamic | Dev Server preview (per sandbox) |

## API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/projects` | Создать проект |
| GET | `/api/projects` | Список проектов |
| DELETE | `/api/projects/{id}` | Удалить проект |
| GET | `/api/projects/{id}/files` | Файлы проекта |
| POST | `/api/chat` | HTTP-чат (без WebSocket) |
| WS | `/ws/chat/{project_id}` | WebSocket чат |
| GET | `/api/llm/status` | Статус LLM провайдеров |
| GET | `/api/health` | Health check |

## LLM провайдеры

| Провайдер | Стоимость (1M tok) | Использование |
|-----------|--------------------|---------------|
| DeepSeek | $0.14-0.55 | Основная работа (~80%) |
| Qwen | $0.15 | Мелкие правки |
| Claude | $3-15 | Vision, code review |
| OpenAI | $2.5-10 | Fallback |

## Следующие шаги (Фаза 2-5)

- [ ] Файловое дерево + Monaco Editor
- [ ] xterm.js терминал
- [ ] Playwright Browser Agent + noVNC визуализация
- [ ] Мультиагентная архитектура (Planner/Coder/Reviewer)
- [ ] Vision: загрузка макетов → код
- [ ] Telegram/VK бот
- [ ] Деплой на хостинг

## Лицензия

MIT
